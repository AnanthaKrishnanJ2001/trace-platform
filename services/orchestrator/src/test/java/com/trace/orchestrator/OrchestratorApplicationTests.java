package com.trace.orchestrator;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — verifies the Spring application context loads with the
 * scaffolded configuration. Expand with real slice/integration tests as
 * controllers, services, and repositories are implemented.
 */
@SpringBootTest
class OrchestratorApplicationTests {

    @Test
    void contextLoads() {
    }
}
